def wait(length=1)
	sleep(rand(1..length))
end

@quotes = [
	"The first rule of fight club is... you do not talk about fight club.",
	"People so angry at me.",
	"... was a promise, I made to Andy..."]

while true
	@randomIndex = rand(@quotes.size)
	@chosenQuote = @quotes[@randomIndex]
	@string = "chat to Stupes " + @chosenQuote.to_s

	send_to_script("lnet", @string + " #reportdreaven")
	sleep(1800)
end
	